#include <stdio.h>
/**
 * main - entry point
 * he numbers of base 16 in lowercase
 * Return: always 0 (Success)
 */

int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (n = 97; m <= 102; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
